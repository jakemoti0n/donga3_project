1. 환경 세팅
1-1. uv 설치 
Windows PowerShell을 관리자 권한으로 실행한 뒤 아래 명령어를 복사해서 입력
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

everything's installed! 문구가 뜨면 설치 완료.

1-2. 저장소 복제 (Clone) 

c 드라이브에 workspace폴더 생성 vscode에서 workspace 열기
VS Code를 열고 터미널(Ctrl+`)에서 아래 명령어입력
ctrl+` 터미널 열기
git clone https://github.com/jakemoti0n/donga3_project.git
cd donga3_project

1-3. 본인 브랜치로 이동반드시 본인의 이니셜 브랜치에서 작업야함
git switch [본인이니셜]
# 예: git switch tj


2. 프로젝트 초기화 및 라이브러리 설정
2-1. 백엔드 (FastAPI) 설정
cd Fastapi
uv sync                               # 팀 공통 라이브러리 자동 설치
.\mude\Scripts\activate               # 가상환경 실행 (프롬프트 앞에 (mude) 확인)

2-2. 프론트엔드 (React/Vite)
cd Frontend
npm install                           # 리액트 라이브러리 설치


3. 깃 세팅

3-1. git pull 받는법
git status 로 본인 브랜치 확인
On branch [본인브랜치]

본인 브랜치 안뜨면
git switch [본인브랜치]

git pull origin main
 

# 라이브러리 업데이트 확인(필요할때만)
# (Fastapi 폴더에서) uv sync
# (mude 폴더에서) npm install

3-2. git push 하는법
git add .
git commit -m "메모"
git push origin [본인브랜치]
# 예: git push origin tj


4. 로컬 실행법
## 가상환경 켜기
cd Fastapi/
# 처음 한 번만: uv sync
.venv\Scripts\activate
# 프롬프트 앞에 (mude)가 뜨는지 확인

## Fast api 서버 실행
uv run uvicorn app.main:app --reload

## 백앤드 실행
cd Backend/mude
# 처음 한 번만: npm install
nodemon server.js

## 프론트엔드 실행
cd Frontend/mude
# 처음 한 번만: npm install
npm run dev

## 가상환경 종료
deactivate