1. uv 설치
## Windows PowerShell 관리자 권한 실행

powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
입력

"
installing to C:\Users\SMHRD\.local\bin
  uv.exe
  uvx.exe
  uvw.exe
everything's installed!
"
이거뜨면 설치완료


uv init (파이썬 인스톨 하라고 뜨면 인스톨)
.\mude\Scripts\activate (명령어)
(mude)앞에 이거붙으면 설치된거



uv venv mude(할필요없음)
mkdir app/api/endpoints, app/services, app/core, data(할필요없음)






2. git 설정
## donga3_project루트에서 .gitignore 생성
내부에 복사 붙여넣기
# Python / FastAPI
**/mude/
**/__pycache__/
**/.env
**/data/*.txt
**/data/*.pdf

# Node / React
**/node_modules/
**/dist/
**/.DS_Store

# IDE
.vscode/
.idea/



git init
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/jakemoti0n/donga3_project.git
git push -u origin main



asdffsdaeawrFADse


## 로컬 실행방법(순서대로 입력)
.\mude\Scripts\activate	 		            (가상환경 실행 명령어)
uv run uvicorn app.main:app --reload 		(fastapi 실행 명령어)
nodemon server.js			                	(backend 실행 명령어)
npm run dev				                      (vitecode 실행명령어)

deactivate(가상환경 나가기)